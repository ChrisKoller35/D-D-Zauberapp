import React from 'react'
import ReactDOM from 'react-dom/client'
import SpellManager from './SpellManager.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SpellManager />
  </React.StrictMode>,
)
